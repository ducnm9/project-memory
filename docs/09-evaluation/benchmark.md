# Evaluation Benchmark

Include onboarding, architecture, why/how, troubleshooting, exact entity lookup, relation, impact, stale/conflict and permission-isolation cases.

Expected evidence IDs should be stored so retrieval can be evaluated independently from answer wording.
